# Root-level env vars used by docker-compose
# Copy this file to .env and fill in values

# --- Frontend (Vite build args for production) ---
VITE_GRAPHQL_URL=http://localhost:4001/graphql
# Full URL of the login page (currently a separate auth app on port 4321)
VITE_LOGIN_URL=http://localhost:4321/login
